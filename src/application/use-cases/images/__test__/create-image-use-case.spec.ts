describe('Create image use case unit test', () => {
    it("Should be able to create a new image", async () =>{
        const imageRepository = new ImageInMemoryRepository()
        const createImageUseCase
    })
});
